---
layout: base.njk
title: Smith Wiki
---
<h1>Smith Wiki</h1>
<p>An auto-grown research wiki — atomic, densely linked pages, written entirely by an agent from public notes.</p>
<ul>
{% for p in collections.kb %}<li><a href="{{ p.url }}">{{ p.data.title }}</a> — <em>{{ p.data.type }}</em></li>
{% endfor %}</ul>
